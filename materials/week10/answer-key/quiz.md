## Answer Key

1. **b) A set of exact rules for writing instructions a computer can
   follow.**
2. **b) Instructions written in a programming language.**
3. **b) Loop.** The same steps repeat, once for each of 200 photos.
4. **c) Decision.** The program picks between two paths: skip or
   rename.
5. **a) Changes code into instructions a chip can run.** That is a
   translator's job, as a compiler or an interpreter.
6. **False.** A computer needs exact, agreed steps; plain English
   leaves room for confusion.
7. **Sequence, loop, and decision.**
8. **Sample answer:** A sequence only does the rename steps once, for
   one photo. Mia has 200 photos, so a loop repeats those same steps
   for each one.
9. **b) `ADD A, 5`.** Assembly language uses short, readable
   mnemonics for each machine instruction.
10. **a) A compiler translates all the code first, before running; an
    interpreter translates one line at a time, while running.** This
    is the key distinction between the two translator styles.
11. **b) The first 149 photos may already be renamed before it
    crashes.** The interpreter runs each line as it goes, so it does
    not catch the typo until it reaches line 150.
12. **b) A class: the blueprint describing what an object of that
    kind will contain.** `Photo` describes the shape; `photo1` and
    `photo2` are the actual objects.
13. **b) An action defined inside a class, that an object of that
    class can perform.** For example, `photo1.rename(...)`.
14. **b) It lets a person describe what result they want, without
    spelling out how to compute it.** SQL never mentions a loop at
    all.
15. **c) JavaScript.** HTML defines structure; CSS defines
    appearance; JavaScript defines behavior.
16. **b) Syntax error.** It breaks the language's own exact rules, so
    the translator refuses to proceed at all.
