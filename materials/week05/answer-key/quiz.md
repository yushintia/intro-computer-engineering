## Answer Key

1. **c) Data in a program you have open right now.** It lives in
   memory while the program runs.
2. **b) A saved file on your hard drive.** Saved data lives in
   storage.
3. **b) It loses its data as soon as power stops.** That is what
   volatile means.
4. **b) Nothing is lost; storage keeps the saved version.** The save
   two minutes ago already copied it into storage.
5. **b) HDD uses a spinning disk; SSD uses chips with no moving
   parts.**
6. **False.** Storage size and speed are different things; a bigger
   drive is not automatically faster.
7. **RAM → SSD → HDD.**
8. **Sample answer:** Unsaved work lives only in memory, which needs
   constant power to keep its data. Saved files live in storage,
   which keeps its data even when the power is off.
